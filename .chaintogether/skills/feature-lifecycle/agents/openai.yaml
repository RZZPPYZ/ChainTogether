interface:
  display_name: "Feature Lifecycle"
  short_description: "Route a feature through its current lifecycle stage."
  default_prompt: "Use $feature-lifecycle to inspect a feature and route it to the correct next stage."
